name: Ping Supabase

on:
  schedule:
    - cron: '0 14 */6 * *'  # every 6 days at 6am PT — keeps free tier from pausing (7-day threshold)
  workflow_dispatch:         # allows manual trigger from GitHub UI

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Supabase
        run: |
          curl -sf -o /dev/null \
            -H "apikey: ${{ secrets.SUPABASE_ANON_KEY }}" \
            "https://fsxqvirnfdweejfsmmwr.supabase.co/rest/v1/poll_votes?limit=1"
